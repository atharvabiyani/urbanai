import { createChatBotMessage } from "react-chatbot-kit";

const config = {
	botName: "Urban AI",
	initialMessages: [createChatBotMessage(`Hi, I'm Urban AI. How can I help you interpret your data?`)],
};

export default config;
